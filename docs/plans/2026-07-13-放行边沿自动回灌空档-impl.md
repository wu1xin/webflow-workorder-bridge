# 放行边沿自动回灌空档 实施计划

> **For Claude:** REQUIRED SUB-SKILL: 用 superpowers:executing-plans 逐任务实现本计划。

**Goal:** 群从「不放行」翻回「放行」（`0→1` 边沿）时，自动对该群做一次「从它已入队水位（无记录则 0）到现在」的单群定向回拉，补齐不放行期间丢失的空档消息。

**Architecture:** `markSynced` 是 `push_allowed` 唯一翻转点，改为返回本轮 `0→1` 的 conversationId；`syncAll` 透出该列表；`SyncService.backfillNewlyAllowed` 对每个新放行群算起点（`queue.MAX(msg_timestamp) ?? 0`）后复用现有 `scheduleRealtimePull` 定向回拉（按 talker 合并防抖、dedup 兜底、不碰全局水位、`ingest_path='sse'`）。SSE 旁路 `onNewGroup`/`onGroupRenamed` 改为返回 Promise 以可 await/可测。

**Tech Stack:** Node.js + TypeScript（ESM）、better-sqlite3、Fastify、Vitest。**代码风格（CLAUDE.md 强约束）**：无分号、单引号；异步优先 Promise 链，仅顺序依赖用 async/await；封装函数直接 `return` Promise、不吞错。每任务完成跑 `npm run lint`。

> 设计依据：[2026-07-13-放行边沿自动回灌空档-design.md](2026-07-13-放行边沿自动回灌空档-design.md)
> 分支：直接在当前 `rewrite/v2` 上实现（不开 worktree）。
> 单文件测试：`npm -w server exec -- vitest run <相对 server 的路径>`；全量：`npm test`（或 `rtk vitest`）。
> **无 schema 迁移、无 `ingest_path` 新枚举、无对接文档变更**（下游 `receiveMessage` 契约不变；SSE 触发 `syncGroups` 已在 2026-07-10 SSE 设计文档中向下游预告）。

---

## Task 1: `chatGroup.markSynced` 返回本轮 `0→1` 边沿

**Files:**
- Modify: `server/src/db/chatGroup.ts:102-110`
- Test: `server/src/db/chatGroup.test.ts`

**Step 1: 写失败测试**

在 `server/src/db/chatGroup.test.ts` 的 `describe('ChatGroupStore', …)` 内追加：

```typescript
    it('markSynced 返回本轮 0→1 边沿：新放行的群，已放行的群不算', () => {
        store.upsertSeen(CH, PF, 'a@chatroom', {}, 1)
        store.upsertSeen(CH, PF, 'b@chatroom', {}, 1)
        store.markSynced(CH, ['a@chatroom'], ['a@chatroom'], 2) // a 先放行
        const edges = store.markSynced(CH, ['a@chatroom', 'b@chatroom'], ['a@chatroom', 'b@chatroom'], 3)
        expect(edges).toEqual(['b@chatroom']) // a 是 1→1（不算），b 是 0→1（算）
    })

    it('markSynced：首次放行未见过裁决的行算 0→1；放行→不放行不算', () => {
        store.upsertSeen(CH, PF, 'a@chatroom', {}, 1)
        expect(store.markSynced(CH, ['a@chatroom'], ['a@chatroom'], 2)).toEqual(['a@chatroom']) // 0→1
        expect(store.markSynced(CH, ['a@chatroom'], [], 3)).toEqual([]) // 1→0，不算
    })
```

**Step 2: 跑测试确认失败**

Run: `npm -w server exec -- vitest run src/db/chatGroup.test.ts`
Expected: FAIL — `markSynced` 返回 `undefined`，`toEqual(['b@chatroom'])` 不通过。

**Step 3: 改 `markSynced` 收集并返回边沿**

`server/src/db/chatGroup.ts`，把 `markSynced` 整体替换为：

```typescript
    /** 白名单回写：sentIds 中命中 allowedIds 置 1、其余置 0，均标记 synced（单事务）。返回本轮 0→1（新放行）的 conversationId。 */
    markSynced(channelId: string, sentIds: string[], allowedIds: string[], now: number): string[] {
        const allow = new Set(allowedIds)
        return this.db.transaction((ids: string[]) => {
            const newlyAllowed: string[] = []
            for (const id of ids) {
                const willAllow = allow.has(id) ? 1 : 0
                const prev = this.isAllowedStmt.get(channelId, id) as { push_allowed: number } | undefined
                if (willAllow === 1 && prev?.push_allowed !== 1) newlyAllowed.push(id) // 0/未知 → 1 为边沿
                this.setAllowStmt.run({ channelId, conversationId: id, allowed: willAllow, now })
            }
            return newlyAllowed
        })(sentIds)
    }
```

**Step 4: 跑测试确认通过**

Run: `npm -w server exec -- vitest run src/db/chatGroup.test.ts`
Expected: PASS（新增 2 条 + 既有全绿——既有调用不接返回值，不受影响）。

**Step 5: lint + 提交**

```bash
npm run lint
git add server/src/db/chatGroup.ts server/src/db/chatGroup.test.ts
git commit -m "feat(db): chatGroup.markSynced 返回本轮 0→1 放行边沿"
```

---

## Task 2: `queue.maxTimestampForConversation`（按群水位）

**Files:**
- Modify: `server/src/db/queue.ts`
- Test: `server/src/db/queue.test.ts`

**Step 1: 写失败测试**

在 `server/src/db/queue.test.ts` 的 `describe('QueueStore', …)` 内追加：

```typescript
    it('maxTimestampForConversation：取该会话最大 msg_timestamp，无记录返回 null', () => {
        store.enqueue(sample({ conversationId: 'g@chatroom', externalId: 's1', msgTimestamp: 100 }), 1)
        store.enqueue(sample({ conversationId: 'g@chatroom', externalId: 's2', msgTimestamp: 300 }), 2)
        store.enqueue(sample({ conversationId: 'other@chatroom', externalId: 's3', msgTimestamp: 999 }), 3)
        expect(store.maxTimestampForConversation('weflow:default', 'g@chatroom')).toBe(300)
        expect(store.maxTimestampForConversation('weflow:default', 'none@chatroom')).toBeNull()
    })
```

**Step 2: 跑测试确认失败**

Run: `npm -w server exec -- vitest run src/db/queue.test.ts`
Expected: FAIL — `maxTimestampForConversation` 不存在。

**Step 3: 加 stmt + 方法**

`server/src/db/queue.ts`：

1）在 `constructor` 字段声明区（`private readonly retryDeadStmt` 附近）加：
```typescript
    private readonly maxTsStmt: BetterSqlite3.Statement
```

2）在 `constructor` 里备语句（放在 `this.retryDeadStmt = …` 之后）：
```typescript
        // 按群水位：该会话已入队消息的最大 msg_timestamp（放行边沿回灌起点，见 2026-07-13-…-design.md §3.2）
        this.maxTsStmt = db.prepare(
            'SELECT MAX(msg_timestamp) AS ts FROM queue WHERE channel_id = ? AND conversation_id = ?',
        )
```

3）加方法（放在 `countByStatus` 附近）：
```typescript
    /** 某会话已入队消息的最大 msg_timestamp（放行边沿回灌起点）；无记录/全空返回 null。 */
    maxTimestampForConversation(channelId: string, conversationId: string): number | null {
        return (this.maxTsStmt.get(channelId, conversationId) as { ts: number | null }).ts
    }
```

**Step 4: 跑测试确认通过**

Run: `npm -w server exec -- vitest run src/db/queue.test.ts`
Expected: PASS。

**Step 5: lint + 提交**

```bash
npm run lint
git add server/src/db/queue.ts server/src/db/queue.test.ts
git commit -m "feat(db): queue 新增 maxTimestampForConversation（按群水位）"
```

---

## Task 3: `groupSyncService.syncAll` 透出 newlyAllowed

**Files:**
- Modify: `server/src/sync/groupSyncService.ts:57-86`
- Test: `server/src/sync/groupSyncService.test.ts`

**Step 1: 写失败测试**

在 `server/src/sync/groupSyncService.test.ts` 的 `describe('GroupSyncService.syncAll')` 内追加：

```typescript
    it('syncAll 返回本轮新放行群（0→1 边沿）；二次同群不再算边沿', async () => {
        const service = svc(() => Promise.resolve({ allowed: ['g1@chatroom'] }))
        const first = await service.syncAll(WEFLOW_CHANNEL_ID, WEFLOW_PLATFORM, [
            { username: 'g1@chatroom', type: 2 }, { username: 'g2@chatroom', type: 2 },
        ])
        expect(first).toEqual(['g1@chatroom'])
        const second = await service.syncAll(WEFLOW_CHANNEL_ID, WEFLOW_PLATFORM, [{ username: 'g1@chatroom', type: 2 }])
        expect(second).toEqual([]) // g1 1→1
    })

    it('syncAll 无群 / 下游失败：返回空数组', async () => {
        const noGroup = await svc(() => Promise.resolve({ allowed: [] }))
            .syncAll(WEFLOW_CHANNEL_ID, WEFLOW_PLATFORM, [{ username: 'wxid_a', type: 1 }])
        expect(noGroup).toEqual([])
        const failed = await svc(() => Promise.reject(new Error('boom')))
            .syncAll(WEFLOW_CHANNEL_ID, WEFLOW_PLATFORM, [{ username: 'g@chatroom', type: 2 }])
        expect(failed).toEqual([])
    })
```

**Step 2: 跑测试确认失败**

Run: `npm -w server exec -- vitest run src/sync/groupSyncService.test.ts`
Expected: FAIL — `syncAll` 返回 `undefined`。

**Step 3: 改 `syncAll` 返回类型与实现**

`server/src/sync/groupSyncService.ts`，把 `syncAll` 整体替换为（改动点：签名 `Promise<void>→Promise<string[]>`、无群返回 `[]`、`.then` 里接住 `markSynced` 返回并透出、`.catch` 返回 `[]`）：

```typescript
    /** 全量：过滤群 → upsert → 发下游 → 回写裁决（无群则不调用下游）。返回本轮 0→1 新放行群。 */
    syncAll(channelId: string, platform: string, sessions: WeflowSession[]): Promise<string[]> {
        const now = this.clock()
        const groups = upsertSeenGroups(this.db, channelId, platform, sessions, now)
        if (groups.length === 0) return Promise.resolve([])

        const sentIds = groups.map(g => g.username)
        const avatarByConv = new Map(this.db.chatGroup.listAll(channelId).map(g => [g.conversationId, g.avatarUrl]))
        const req: SyncGroupsRequest = {
            agentId: channelId,
            platform,
            groups: groups.map(g => ({
                sessionId: g.username,
                groupName: g.displayName ?? null,
                avatarUrl: avatarByConv.get(g.username) ?? null,
                lastMessageAt: g.lastTimestamp ?? null,
            })),
        }
        return this.downstream.syncGroups(req)
            .then(({ allowed }) => {
                const newlyAllowed = this.db.chatGroup.markSynced(channelId, sentIds, allowed, this.clock())
                this.log.info({ sent: sentIds.length, allowed: allowed.length, newlyAllowed: newlyAllowed.length }, '[group-sync] 群同步完成')
                return newlyAllowed
            })
            .catch((e: unknown) => {
                const message = e instanceof Error ? e.message : String(e)
                this.db.chatGroup.markSyncFailed(channelId, sentIds, message, this.clock())
                this.log.error({ err: message }, '[group-sync] 群同步失败，本轮裁决保持原值')
                this.alert.send({ level: 'warn', type: 'group_sync_failed', title: '群同步下游失败', message })
                return []
            })
    }
```

**Step 4: 跑测试确认通过**

Run: `npm -w server exec -- vitest run src/sync/groupSyncService.test.ts`
Expected: PASS（新增 2 条 + 既有全绿）。

> 注：此步后 `SyncService` 里对 `groupSync.syncAll(...)` 的 `void`/`await` 调用编译仍通过（`void` 忽略返回、`await` 兼容），行为不变——真正消费返回值在 Task 4。

**Step 5: lint + 提交**

```bash
npm run lint
git add server/src/sync/groupSyncService.ts server/src/sync/groupSyncService.test.ts
git commit -m "feat(group-sync): syncAll 透出本轮新放行群 newlyAllowed"
```

---

## Task 4: `SyncService` 回灌主体 + 接线 + SSE 旁路可 await

**Files:**
- Modify: `server/src/sync/syncService.ts`
- Test: `server/src/sync/syncService.test.ts`

**Step 1: 升级测试桩到新签名（先让既有测试在新签名下仍绿）**

`server/src/sync/syncService.test.ts` 顶部与两处 `spyGroupSync`——把 `syncAll` 桩返回 `Promise.resolve()` 全部改为 `Promise.resolve([])`，并把 `deps` 的 groupSync 类型改成返回 `Promise<string[]>`：

1）顶部（约 21 行）：
```typescript
// 群同步桩：默认 no-op（测试自己预置 chat_group 放行）
const noopGroupSync = { syncAll: () => Promise.resolve([] as string[]) }
```

2）`deps` 签名（约 23 行）：
```typescript
function deps(db: Db, client: ReturnType<typeof stubClient>, groupSync: { syncAll: () => Promise<string[]> } = noopGroupSync) {
```

3）「系统消息分发」describe 的 `spyGroupSync`（约 400 行）与「群生命周期旁路」describe 的 `spyGroupSync`（约 470 行）两处，`syncAll` 回调结尾 `return Promise.resolve()` 改为 `return Promise.resolve([] as string[])`。

**Step 2: 跑既有测试确认仍绿**

Run: `npm -w server exec -- vitest run src/sync/syncService.test.ts`
Expected: PASS（纯桩签名调整，无行为变化）。

**Step 3: 写失败测试（新行为）**

在 `server/src/sync/syncService.test.ts` 顶部 import 补 `GroupSyncService`：
```typescript
import { GroupSyncService } from './groupSyncService.js'
```

在「群生命周期旁路」describe **之后**新增一个 describe：

```typescript
describe('SyncService 放行边沿自动回灌（backfillNewlyAllowed）', () => {
    let db: Db
    beforeEach(() => { db = Db.openMemory() })
    afterEach(() => db.close())

    const gsLog = { info() {}, warn() {}, error() {}, debug() {} } as never

    it('backfillNewlyAllowed：起点=该群 queue 最大 msg_timestamp（无记录则 0）', async () => {
        allowGroup(db, 'proj@chatroom')
        db.queue.enqueue({
            channelId: WEFLOW_CHANNEL_ID, platform: WEFLOW_PLATFORM, eventType: 'message.new',
            externalId: 's-old', conversationId: 'proj@chatroom', senderId: null, senderName: null, senderAvatar: null,
            msgTimestamp: 250, hasMedia: 0, rawJson: '{}', mediaJson: null, ingestPath: 'sse', revocableUntil: null,
        }, 1)
        let seenStart = -1
        const client = {
            listSessions: () => Promise.resolve([] as WeflowSession[]),
            fetchMessagesPage: (_t: string, start: number) => { seenStart = start; return Promise.resolve({ messages: [], hasMore: false }) },
            fetchMembers: () => Promise.resolve({ groupName: null, groupAvatar: null, members: new Map() }),
        }
        const svc = new SyncService(deps(db, client as never))
        await svc['backfillNewlyAllowed'](['proj@chatroom'])
        expect(seenStart).toBe(250)

        await svc['backfillNewlyAllowed'](['fresh@chatroom']) // 无 queue 记录
        expect(seenStart).toBe(0)
    })

    it('空列表：no-op、不发 REST', async () => {
        let calls = 0
        const client = {
            listSessions: () => Promise.resolve([] as WeflowSession[]),
            fetchMessagesPage: () => { calls += 1; return Promise.resolve({ messages: [], hasMore: false }) },
            fetchMembers: () => Promise.resolve({ groupName: null, groupAvatar: null, members: new Map() }),
        }
        const svc = new SyncService(deps(db, client as never))
        await svc['backfillNewlyAllowed']([])
        expect(calls).toBe(0)
    })

    it('端到端：新入群下游放行 → backfill 从 0 全量回灌该群历史（ingest_path=sse）', async () => {
        const client = stubClient([], { 'proj@chatroom': { messages: [
            { serverId: 's1', createTime: 100, content: 'a' },
            { serverId: 's2', createTime: 200, content: 'b' },
        ], hasMore: false } })
        const groupSync = new GroupSyncService({
            db, downstream: { syncGroups: () => Promise.resolve({ allowed: ['proj@chatroom'] }) } as never,
            log: gsLog, alert: { send() {} }, now: () => 1,
        })
        const svc = new SyncService({ ...deps(db, client), groupSync })

        // 未知群 SSE → onNewGroup 建行(不放行) → syncAll 下游放行(0→1) → backfill start=0 全量拉
        await svc.ingestRealtime(groupSse('proj@chatroom', {}))

        expect(db.queue.countByStatus('pending')).toBe(2)
        const { items } = db.queue.list(WEFLOW_CHANNEL_ID, {}, 10, 0)
        expect(items.every(i => i.ingestPath === 'sse')).toBe(true)
    })

    // groupSse helper：与「群生命周期旁路」describe 内同款，复制一份到本 describe
    function groupSse(sessionId: string, opts: { content?: string, sessionType?: string, avatarUrl?: string | null, ts?: number } = {}) {
        const { content = 'hi', sessionType = 'group', avatarUrl = null, ts = 100 } = opts
        return { event: 'message.new', data: JSON.stringify({ event: 'message.new', sessionId, sessionType, avatarUrl, content, timestamp: ts }) }
    }
})
```

**Step 4: 跑测试确认失败**

Run: `npm -w server exec -- vitest run src/sync/syncService.test.ts`
Expected: FAIL — `backfillNewlyAllowed` 不存在 / 端到端未回灌（0 pending）。

**Step 5: 实现 `backfillNewlyAllowed` + SSE 旁路可 await + 接线**

`server/src/sync/syncService.ts`：

1）新增私有方法 `backfillNewlyAllowed`（放在 `scheduleRealtimePull` 之前）：
```typescript
    /**
     * 放行边沿回灌：对每个新放行群，从其 queue 已入队水位（无记录则 0）定向回拉到现在，
     * 补齐不放行期间丢失的空档。复用 scheduleRealtimePull（按 talker 合并、dedup 兜底、不碰全局水位）。
     */
    private backfillNewlyAllowed(convIds: string[]): Promise<void> {
        if (convIds.length === 0) return Promise.resolve()
        return Promise.all(convIds.map((conv) => {
            const start = this.db.queue.maxTimestampForConversation(WEFLOW_CHANNEL_ID, conv) ?? 0
            this.log.info({ conv, start }, '[sync] 放行边沿，回灌该群空档')
            return this.scheduleRealtimePull(conv, start)
        })).then(() => {})
    }
```

2）`onNewGroup` 改为返回 Promise 并接 backfill（整体替换）：
```typescript
    /**
     * 新入群：先 upsertSeen 建行（同步、含可信头像 → 满足 exists 防抖 + 前端可见），配了下游再单群裁决。
     * 群名从 SSE 不可信，故不带 displayName，由下一轮消息同步的 chatlab meta 回补。
     * 返回 Promise：syncAll 裁决 → 若翻放行(0→1)则 backfillNewlyAllowed 回灌该群历史（首次开通 start=0 全量）。
     */
    private onNewGroup(talker: string, avatarUrl: string | null, now: number): Promise<void> {
        this.log.info({ talker }, '[sync] 检测到新入群')
        this.db.chatGroup.upsertSeen(WEFLOW_CHANNEL_ID, WEFLOW_PLATFORM, talker, { avatarUrl }, now)
        if (!this.groupSync) return Promise.resolve()
        const session: WeflowSession = { username: talker, type: 2 }
        return this.groupSync.syncAll(WEFLOW_CHANNEL_ID, WEFLOW_PLATFORM, [session])
            .then(newlyAllowed => this.backfillNewlyAllowed(newlyAllowed))
    }
```

3）`onGroupRenamed` 改为返回 Promise 并接 backfill（整体替换）：
```typescript
    /**
     * 群改名：以新名回推下游单群（复用 groupSync.syncAll，其内部先覆盖本地名再发下游、按白名单重新裁决）。
     * 返回 Promise：若重裁把群从不放行翻为放行(0→1)则 backfillNewlyAllowed 回灌该群空档。
     * 错误由 syncAll 内部自吞（标 failed、不误关裁决、告警）。未配置下游时仅更新本地群名。
     */
    private onGroupRenamed(conversationId: string, newName: string, now: number): Promise<void> {
        this.log.info({ conversationId, newName }, '[sync] 检测到群改名')
        if (!this.groupSync) {
            this.db.chatGroup.upsertSeen(WEFLOW_CHANNEL_ID, WEFLOW_PLATFORM, conversationId, { groupName: newName }, now)
            return Promise.resolve()
        }
        const session: WeflowSession = { username: conversationId, displayName: newName, type: 2 }
        return this.groupSync.syncAll(WEFLOW_CHANNEL_ID, WEFLOW_PLATFORM, [session])
            .then(newlyAllowed => this.backfillNewlyAllowed(newlyAllowed))
    }
```

4）`dispatchSystemEvent` 里对 `onGroupRenamed` 的调用改为 `void`（现在返回 Promise，放行群改名 backfill 恒空、无害；`void` 显式丢弃避免 floating promise）：
```typescript
            case 'group_renamed':
                void this.onGroupRenamed(talker, event.newName, now)
                break
```

5）`detectGroupLifecycle` 改为返回 Promise（整体替换）：
```typescript
    private detectGroupLifecycle(env: SseEnvelope): Promise<void> {
        const now = nowSec()
        if (!this.db.chatGroup.exists(WEFLOW_CHANNEL_ID, env.talker)) {
            return this.onNewGroup(env.talker, env.avatarUrl, now)
        }
        if (!this.db.chatGroup.isPushAllowed(WEFLOW_CHANNEL_ID, env.talker)) {
            const newName = matchGroupRename(env.content)
            if (newName) return this.onGroupRenamed(env.talker, newName, now)
        }
        return Promise.resolve()
    }
```

6）`ingestRealtime` 合并 lifecycle Promise（整体替换 `if (isGroupSession…)` 起至 `return this.scheduleRealtimePull(…)`）：
```typescript
        // 群生命周期旁路（放行闸门之前、同步前缀执行 upsertSeen 建行以防抖，仅群会话）
        const lifecycle = isGroupSession(env) ? this.detectGroupLifecycle(env) : Promise.resolve()
        if (!this.db.chatGroup.isPushAllowed(WEFLOW_CHANNEL_ID, env.talker)) {
            return lifecycle // 非放行群：不发实时回查；但等待重裁+可能的回灌完成（可测/错误传播）
        }
        return Promise.all([lifecycle, this.scheduleRealtimePull(env.talker, env.ts)]).then(() => {})
```

> 关键：`detectGroupLifecycle` 的同步前缀（`onNewGroup`/`onGroupRenamed` 内的 `upsertSeen`）在返回 Promise 前已同步执行，防抖不变；`isPushAllowed` 紧随其后读到的仍是翻转前的值（`syncAll` 在微任务里才翻），故新群 SSE 当下走非放行分支、由 backfill 从 0 全量拉，而非用 `env.ts` 起拉。

7）私有 `syncGroups` 透出 newlyAllowed（整体替换）：
```typescript
    /** 列会话后同步群到下游；未配置 groupSync 时仍把所有群入库（默认不放行、本轮不入队）。返回本轮新放行群。 */
    private syncGroups(sessions: WeflowSession[]): Promise<string[]> {
        if (!this.groupSync) {
            upsertSeenGroups(this.db, WEFLOW_CHANNEL_ID, WEFLOW_PLATFORM, sessions, nowSec())
            this.log.warn('[sync] 未配置下游群同步，所有群已入库但默认不放行、本轮不入队')
            return Promise.resolve([])
        }
        return this.groupSync.syncAll(WEFLOW_CHANNEL_ID, WEFLOW_PLATFORM, sessions)
    }
```

8）`runFullSync` 里 `await this.syncGroups(sessions)` **保持不变**（忽略返回值——全量已 start=0 拉过所有放行群，不重复回灌）。

9）`runCompensation` 接住 newlyAllowed 并在主循环后回灌。把 `await this.syncGroups(sessions)` 改为接收返回值，并在 `this.finish()` 之后 `await` 回灌：
```typescript
            const newlyAllowed = await this.syncGroups(sessions)
```
```typescript
            this.advanceWatermark(watermark)
            this.finish()
            await this.backfillNewlyAllowed(newlyAllowed) // 边沿群空档在全局水位之下，主循环拉不到，单独按群水位补
            this.log.info({ enqueued: this.progress.enqueued, duplicates: this.progress.duplicates, since: start }, '[sync] 补偿同步完成')
```

10）`syncGroupsNow` 接 backfill（把 `.then((sessions) => {…})` 段整体替换为下面两个 `.then`）：
```typescript
            .then((sessions) => {
                if (groupSync) return groupSync.syncAll(WEFLOW_CHANNEL_ID, WEFLOW_PLATFORM, sessions)
                upsertSeenGroups(this.db, WEFLOW_CHANNEL_ID, WEFLOW_PLATFORM, sessions, nowSec())
                return [] as string[]
            })
            .then((newlyAllowed) => this.backfillNewlyAllowed(newlyAllowed))
```

**Step 6: 跑测试确认通过**

Run: `npm -w server exec -- vitest run src/sync/syncService.test.ts`
Expected: PASS（新 describe 全绿 + 既有全绿）。

**Step 7: lint + 提交**

```bash
npm run lint
git add server/src/sync/syncService.ts server/src/sync/syncService.test.ts
git commit -m "feat(sync): 放行 0→1 边沿自动回灌该群空档（复用定向回拉）"
```

---

## Task 5: 全量校验

**Step 1: 全量测试 + lint**

Run:
```bash
npm test
npm run lint
```
Expected: 全绿、无 lint 报错。

**Step 2: 类型编译校验**

Run: `npm -w server run build`
Expected: tsc 无报错。

**Step 3: 若有零散改动则收尾提交**

```bash
git add -A
git commit -m "chore: 放行边沿回灌收尾（校验通过）"
```

---

## 验收标准（Definition of Done）

- [ ] `npm test` 全绿、`npm run lint` 无报错、`npm -w server run build` tsc 无报错。
- [ ] `markSynced` 返回本轮 `0→1` 边沿；`1→1`/`1→0`/`markSyncFailed` 不产生边沿。
- [ ] `queue.maxTimestampForConversation` 取该会话最大 `msg_timestamp`、无记录返回 `null`、跨会话隔离。
- [ ] `syncAll` 透出 newlyAllowed；无群 / 下游失败返回 `[]`。
- [ ] 端到端：未知群 SSE → 下游放行 → 该群历史从 0 全量回灌入队（`ingest_path=sse`）；空列表 no-op、不发 REST。
- [ ] `backfillNewlyAllowed` 起点 = 该群 queue 水位（无则 0）。
- [ ] 既有 SSE/改名/新入群/撤回/全量/补偿测试全部保持绿（桩签名同步升级为返回 `[]`）。
- [ ] `runFullSync` 不触发回灌（避免重复）。
