<!--
  这是 XAML 文件（读作 "zammel"）。XAML 是 WPF 用来"声明式描述界面/资源"的 XML 标记语言，
  作用类似网页里的 HTML+CSS：你用标签写出界面长什么样，C# 那边（.xaml.cs）写交互逻辑。
  本文件 App.xaml 是"应用级"定义（不是某个窗口），主要放全局资源，比如主题、样式。
  XAML/XML 的注释格式，就是你正在读的这种由「尖括号、叹号、两个连字符」开头、并对称收尾的块。
  （正因如此，注释"内部"不能再出现两个连续的连字符，否则会被当成注释提前结束而报错。）

  ⚠ 重要语法限制：XML 注释只能出现在标签"之间"，不能塞进某个标签的属性中间。
     所以下面 <Application> 开始标签里那一堆属性，只能在这里集中解释：

     · x:Class="WeflowAgent.App.App"
         把这个 XAML 和 C# 里的 partial class App 绑成同一个类（见 App.xaml.cs 里 partial 的说明）。

     下面几行 xmlns 是"XML 命名空间声明"，作用等同 C# 的 using：先声明、才能用对应标签。
     · xmlns="...winfx/2006/xaml/presentation"
         默认命名空间：WPF 自带标准控件（Button、Grid、TextBlock…）都来自这里，用时不加前缀。
     · xmlns:x="...winfx/2006/xaml"
         前缀 x: 指向 XAML 语言本身的关键字（如 x:Class、后面会见到的 x:Name）。
     · xmlns:ui="...lepo.co/wpfui/2022/xaml"
         前缀 ui: 指向第三方库 WPF UI（lepoco），提供 Fluent（Win11 风格）外观控件。
     · xmlns:local="clr-namespace:WeflowAgent.App"
         前缀 local: 指向我们自己项目的命名空间，引用自己写的类型时用它。
-->
<Application x:Class="WeflowAgent.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:WeflowAgent.App">

    <!-- Application.Resources：定义"全应用共享"的资源字典；任何窗口都能取用里面的东西。 -->
    <Application.Resources>
        <ResourceDictionary>
            <!-- MergedDictionaries：把多个资源字典"合并"进来，等于一次性引入多套预置资源。 -->
            <ResourceDictionary.MergedDictionaries>
                <!-- 引入 WPF UI 的主题，并选用 Dark（深色）主题。 -->
                <ui:ThemesDictionary Theme="Dark" />
                <!-- 引入 WPF UI 各控件的默认样式（不引入则控件会没样式）。 -->
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
